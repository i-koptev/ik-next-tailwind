```bash
$ nvm install 14.16.0
$ nvm use 14.16.0
$ npx create-next-app ik-next-tailwind
$ cd ik-next-tailwind
$ rm -rf ./pages/api ?
$ touch  .nvmrc
$ touch  .eslintrc.json
$ touch  .prettierrc
$ touch  .prettierignore
$ touch tsconfig.json
$ yarn add --dev typescript @types/node @types/react @types/react-dom
$ yarn add -D tailwindcss@latest postcss@latest autoprefixer@latest
$ npx tailwindcss init --> Created Tailwind config file: tailwind.config.js
$ touch postcss.config.js

```
